// Steven ainda está construindo sua calculadora de dicas,
// usando as mesmas regras de antes: Dica 15% de
// a conta se o valor da conta estiver entre 50 e 300,
// e se o valor for diferente, a gorjeta é 20%.

// Suas tarefas:
// 1. Escreva uma função 'calcTip' que recebe qualquer valor de fatura como entrada e retorna
// a dica correspondente, calculada com base nas regras acima (você pode conferir
// o código do primeiro desafio de calculadora de ponta, se necessário). Use a função
// tipo que você mais gosta. Teste a função usando um valor de conta de 100
// 2. E agora vamos usar arrays! Portanto, crie uma matriz de 'contas' contendo os dados de teste
// abaixo
// 3. Crie uma matriz de 'dicas' contendo o valor da gorjeta para cada conta, calculado a partir de
// a função que você criou antes
// 4. Bônus: Crie uma matriz 'total' contendo os valores totais, então a conta + gorjeta

// Dados de teste: 125, 555 e 44

// Dica: Lembre-se de que uma matriz precisa de um valor em cada posição, e esse valor pode
// realmente é o valor retornado de uma função! Então você pode simplesmente chamar uma função como array
// valores (portanto, não armazene os valores de ponta em variáveis ​​separadas primeiro, mas diretamente no novo
// array)
